name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.5.4
      with:
        ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

    - name: Copy files via SCP
      run: |
        scp -o StrictHostKeyChecking=no -r . ec2-user@seu-endereco-ec2.amazonaws.com:/home/ec2-user/app

    - name: SSH and deploy
      run: |
        ssh -o StrictHostKeyChecking=no ec2-user@seu-endereco-ec2.amazonaws.com << 'EOF'
          cd /home/ec2-user/app
          sudo docker-compose down
          sudo docker-compose up -d
        EOF
